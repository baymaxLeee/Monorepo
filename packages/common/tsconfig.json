{
  "extends": "../../tsconfig.json", // 继承根目录的配置
  "compilerOptions": {
    "outDir": "./dist", // 输出目录
    "rootDir": "./src", // 源代码目录
    "baseUrl": ".", // 基础路径
    "paths": {
      "@monorepo/common": ["./src"] // 路径别名
    }
  },
  "include": ["src"], // 包含的文件
}